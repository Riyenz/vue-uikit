import { APP_NAME } from '../config';

export const COMPONENT_NAME = 'Dropdown';
export const DROPDOWN_CLASS_NAME = 'eduk-dropdown';
export const DROPDOWN_PLACEMENT = {
  LEFT: 'left',
  CENTER: 'center',
  RIGHT: 'right',
};
export const ERROR_MESSAGE = `${APP_NAME} ${COMPONENT_NAME}: this is a sample error message`;
