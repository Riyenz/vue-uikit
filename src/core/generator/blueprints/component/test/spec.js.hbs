import { expect } from 'chai';
import { shallowMount } from '@vue/test-utils';
import {{ scopeName }} from '@/UIKit/{{ scopeName }}/{{ scopeName }}.component.vue';
import { {{ toUpperCase scopeName }}_CLASS_NAME } from '@/UIKit/{{ scopeName }}/{{ scopeName }}.config';

describe('{{ scopeName }}.component.vue', () => {
  it('adds a .eduk-no-gutters class when props noGutters is provided', () => {
    const wrapper = shallowMount({{ scopeName }});

    expect(wrapper.find(`.${ {{ toUpperCase scopeName }}_CLASS_NAME }`).exists()).toBe(true);
  });
});
