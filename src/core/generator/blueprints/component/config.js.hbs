import { APP_NAME } from '../config';

export const COMPONENT_NAME = '{{ scopeName }}';
export const {{ toUpperCase scopeName }}_CLASS_NAME = 'eduk-{{ toLowerCase scopeName }}';
export const ERROR_MESSAGE = `${APP_NAME} ${COMPONENT_NAME}: this is a sample error message`;
